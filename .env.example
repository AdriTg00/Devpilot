# === LLM Backend ===
OLLAMA_MODEL=qwen2.5-coder:7b
GROQ_API_KEY=
GROQ_MODEL=fast

# === Auth ===
JWT_SECRET=change-me-in-production

# === CORS (origenes permitidos, separados por coma) ===
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# === Database (SQLite, default auto) ===
# DATABASE_URL=sqlite:///./.memory/devpilot.db

# === Persistencia ===
MEMORY_STORAGE_PATH=.memory/conversations.json
# SHARES_STORAGE_PATH=.memory/shares.json

# === URL base para links de sharing ===
# BASE_URL=http://localhost:5173

# === Docker (opcional) ===
# COMPOSE_PROJECT_NAME=devpilot
